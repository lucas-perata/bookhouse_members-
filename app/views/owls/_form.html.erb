<%= simple_form_for (@owl) do |form| %>
  <% if @owl.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(owl.errors.count, "error") %> prohibited this owl from being saved:</h2>

      <ul>
        <% owl.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :owl, style: "display: block" %>
    <%= form.text_area :owl %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
